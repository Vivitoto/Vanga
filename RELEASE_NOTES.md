# Vanga 0.1.10

本版本重点整理设置界面与 Vanga 本地收藏 WebDAV 同步体验，统一圆角和间距，并修正移动端阅读器设置里的高级选项位置。

## 收藏同步

- 收藏同步改为两个明确方向：`同步至本地` 和 `同步至云端`。
- `同步至本地` 会从 WebDAV 合并到本机，不上传本机收藏。
- `同步至云端` 会以当前本机收藏为准上传到 WebDAV。
- WebDAV 同步文件继续按服务器和账号分开存放，避免多用户或多服务器互相覆盖。
- 同步成功提示现在会明确显示同步方向。

## 设置界面

- 统一设置页卡片、输入框、下拉框、按钮和常见控件的圆角。
- 收紧设置页间距，让移动端和桌面端排版更接近 Visto 风格。
- 设置项改为更统一的行式布局，减少说明文字和控件上下分裂的问题。
- 删除大量显而易见的说明文案，只保留 WebDAV、危险操作、重启影响、缓存/性能、Komf 模板等容易误解或有副作用的说明。
- 优化设置侧边导航分组与副标题，减少冗余信息。
- 避免展开入口和展开后的分区使用相同标题，降低阅读混乱。

## 阅读器设置

- 图片阅读器普通设置只保留基础显示项。
- 翻页闪屏相关选项移动到高级设置中。
- 移除用户可见的“墨水屏 / 残影”表述，避免让普通用户误解为专用设备功能。
- 手机底部设置弹层和侧边设置菜单的顺序保持一致：图片显示、图片采样、高级设置。

## 界面一致性

- 统一封面卡片、搜索、顶部栏、批量操作、对话框、分页、筛选器等常见界面的圆角表现。
- 下拉输入、文本输入、密码输入、HTTP 地址输入、数字输入等表单控件使用统一形状。
- 保留必要的局部圆角结构，例如弹层顶部圆角和卡片局部圆角。

## 说明

这是 `0.1.9` 之后的界面整理与同步体验修复版本，重点处理设置页观感、WebDAV 收藏同步方向语义，以及阅读器高级选项的可理解性。
