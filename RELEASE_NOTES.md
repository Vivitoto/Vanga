# Vanga 1.0.1

这是 Vanga 的 1.0.1 修复版本，重点修复 Komf 元数据识别进度连接超时时可能导致应用退出的问题。

## Komf 识别

- 修复 Komf 识别进度 SSE 事件流超时或网络异常时，异常从子协程冒出导致 Android 应用退出的问题。
- SSE 超时现在会显示错误通知，并安全关闭识别流程，而不是让整个应用崩溃。
- 保留正常识别完成、任务已完成、任务不存在等原有处理逻辑。
- 保留协程取消语义，关闭弹窗/释放 ViewModel 时不会误吞正常取消。

## 说明

如果 Komf 访问外部数据源本身仍然超时，Vanga 会显示错误提示；这通常需要检查 Komf 容器的外网连通性、数据源配置或代理设置。
