# Vanga 0.1.11

本版本修复移动端在首页、书库、收藏、设置之间切换时的书库保活体验，并减少 Komga 扫描/事件刷新导致的整页 Loading。

## 移动端导航

- 移动端顶层页面切换时保留首页、书库、收藏和设置页面内容。
- 从其他 tab 返回书库时优先显示已有内容，不再每次重新进入整页 Loading。
- 书库页在移动端切换顶层 tab 时保持当前库、子 tab 和页面状态。

## 书库刷新

- 书库可见时继续响应 Komga SSE 事件刷新。
- 书库不可见时不立即发起网络刷新，只标记待刷新状态。
- 用户切回书库后再合并执行一次刷新，避免后台事件频繁打断当前界面。
- 事件刷新尽量保留当前内容，不把已有列表清空成全屏 Loading。

## 搜索和批量操作

- 同步适配当前主线的批量操作栏接口，保持搜索结果批量选择可编译可用。

## 说明

这是 `0.1.10` 之后的移动端书库体验修复版本，重点处理 tab 切换保活和 Komga 事件刷新时机。
