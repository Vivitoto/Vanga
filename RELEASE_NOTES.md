# Vanga 1.0.5

这是 Vanga 的 1.0.5 版本，重点改进多屏幕尺寸下的响应式布局体验。

## 响应式布局

- 卡片网格和列表网格从固定列/宽度改为自适应布局，在不同窗口宽度下自动调整列数和卡片尺寸。
- 封面宽度根据可用空间动态 clamp，避免窄窗口下封面溢出。
- 底部批量操作浮层在窄窗口下正确避让，不再遮挡内容。
- 设置页和 KOMF 详情页在窄窗口下自动切换为单列布局，避免双栏挤压。
- 阅读器进度预览缩略图根据窗口尺寸自动缩放。
- 按钮组从固定 Row 改为 FlowRow，窄屏下自动换行不溢出。
- 修复设置页双栏布局在特定宽度区间（~540-553px）内容区被压缩到最小宽度以下的问题。
- 弹窗宽度使用显式响应式逻辑，小窗口取比例、大窗口封顶，避免宽度计算歧义。

## 版本

- 更新应用版本为 1.0.5，Android versionCode 为 15。
