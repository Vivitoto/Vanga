# Vanga 1.0.0

这是 Vanga 的 1.0.0 版本，继续打磨移动端主界面信息密度，让底部五个 tab 的顶部留白和视觉结构更一致。

## 移动端界面

- 统一首页、书库、收藏、搜索、设置五个底部 tab 的顶部内容间距。
- 移动端首页、收藏和设置页去掉顶部大标题/说明块，减少重复导航信息。
- 搜索页保留搜索框，并与其他 tab 使用一致的顶部留白。

## 书库页

- 移除移动端书库顶部的 `全部书库` / 书库名称标题。
- 移除作品列表区域的 `作品列表` 标题。
- 将作品总数改为更轻的圆角数量标签，例如 `123 部作品`。
- 保留有权限时的书库操作菜单，不影响管理员书库管理入口。

## 设置页

- 移除移动端设置页顶部标题和分割线，使列表直接进入内容。
- 为设置列表底部增加留白，避免 `退出登录` 紧贴底部导航栏。

## 说明

这是 `0.1.11` 之后的移动端 UI 密度与间距修正版本，重点处理设置页底部拥挤、书库页顶部冗余标题，以及底部 tab 之间顶部间距不一致的问题。
