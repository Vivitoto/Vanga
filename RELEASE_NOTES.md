# Vanga 1.0.4

这是 Vanga 的 1.0.4 修复版本，重点改进外部数据源被 Cloudflare 或 HTML 错误页拦截时的应用内提示。

## 数据源错误提示

- 当外部数据源返回 Cloudflare 拦截页时，显示更清晰的中文提示，提醒检查代理出口或使用可通过 Cloudflare 的 komf-aver 访问方式。
- 当服务端返回 HTML 错误页时，避免直接展示整段 HTML，改为提示检查目标服务、代理或登录状态。
- 普通异常消息也会经过用户可读化处理，减少把原始网页错误内容直接暴露给用户。

## 版本

- 更新应用版本为 1.0.4，Android versionCode 为 14。
