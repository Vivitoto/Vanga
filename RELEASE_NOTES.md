# Vanga 0.1.8

版本信息修正，并包含 0.1.7 的移动端界面和阅读翻页体验优化。

## 版本修正

- 修正应用内部“当前版本”仍显示/判断为 `0.1.6` 的问题，避免 0.1.7 安装后继续提示更新到同版本。
- 新增 CI 版本一致性检查，防止 `gradle/libs.versions.toml` 与 `AppVersion.current` 再次不一致。

## 阅读体验

- 优化漫画 paged 阅读器的横向滑动翻页：
  - 拖动时当前页会跟随手指移动。
  - 相邻页会在同一条横向轨道上预览衔接。
  - 松手后超过阈值会继续平滑补齐到下一页，未超过阈值则平滑回弹。
  - 已预加载的相邻页会直接显示真实内容，未加载完成时才显示占位。
  - 左边缘返回手势不再带动页面一起拖动，减少误触冲突。

## 移动端筛选

- 重排手机宽度下的书库筛选面板：
  - 搜索框单独占满一行。
  - “重置筛选 / 隐藏筛选”按钮独立成行并等宽显示。
  - 筛选下拉项改为全宽单列布局，避免挤压和错位。
- 默认全部书库状态下不再显示冗余的“全部书库”标题，只在选中特定书库时显示库名。
- 筛选下拉框使用 compact 高度，同时保留设置页普通下拉框的 56dp 行高。

## 界面优化

- 继续统一列表、卡片、设置页和空状态的移动端排版节奏。
- 合集页标题区去除旧式 `collection` 标签，改为更像 App 的标题与统计信息。
- 阅读清单和详情页的信息/操作行改为“左侧说明、右侧操作”的布局，减少元素全部靠左堆叠造成的后台网页感。
- 增大合集、书库、阅读清单等封面网格的卡片间距，避免卡片贴在一起。
- 移动端封面卡片常显阅读/更多等小操作入口，不再依赖桌面 hover 才能发现操作。
- 单本漫画页将合集名、册数页数和阅读/下载操作合并为自适应信息操作带，减少网页元素罗列感。
- 优化批量操作、分页、导航菜单、下载弹窗、搜索页、合集/阅读列表/书籍列表等区域的间距和对齐。
- 微调内置 EPUB reader 标题文案为 Vanga。

## 说明

这是 `0.1.7` 之后的热修版本，同时保留 0.1.7 的移动端筛选、阅读滑动翻页和 UI 排版优化。
