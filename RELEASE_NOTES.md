# Vanga 1.0.12

这次更新修复跨页面导航后的滚动位置丢失问题。

## 改进

- 新增 `ScrollStatePreserver`，用页面级 key 缓存列表/网格滚动位置。
- 跨页面导航返回系列列表时保留原滚动位置，避免从顶部重新浏览。
- 系列列表复用可恢复的 LazyGridState，适配页面替换场景下的状态恢复。

## 版本

- 更新应用版本为 1.0.12，Android versionCode 为 22。

---

# Vanga 1.0.11

这次更新优化系列详情里的书籍区域视图切换体验。

## 改进

- 书籍工具栏新增视图快速切换下拉菜单。
- 可直接选择列表、紧凑网格、标准网格或舒适网格，不需要反复点击循环切换。
- 保留原有列表/网格循环切换按钮行为。
- 网格密度继续复用现有封面宽度设置，不新增额外持久化选项。

## 版本

- 更新应用版本为 1.0.11，Android versionCode 为 21。

---

# Vanga 1.0.10

修复 1.0.9 引入的 UI 回归问题。

## 修复

- 阅读进度条恢复到 1.0.8 样式（去掉多余底部间距）
- 阅读按钮上移 3dp，避免与进度条重叠
- 仅当封面卡片有阅读按钮时才上移标题
- 账号页间距收紧，减少留白
- 外观页「封面宽度」标签与值恢复同行排列
- 外观页封面尺寸预览卡片添加背景色，更显眼
- 离线模式下载与存储行恢复同行排列
- 用户管理卡加「用户」标签胶囊
- 高级设置移除多余的描述文字

## 版本

- 更新应用版本为 1.0.10，Android versionCode 为 20。

# Vanga 1.0.9

这是 Vanga 的 1.0.9 版本，主题是"消灭左撇子设计"——全面修复设置页、账号页和信息区的视觉偏左问题。

## 全局设计修正

- 所有设置页的 `SettingsValueRow` 改为堆叠式布局：标签在上方、值在下方右对齐，不再有标签与值之间的大片空白。
- 所有设置行的水平内边距统一增加到 6dp，标签不再紧贴卡片边缘。
- 漫画详情页的元信息行（胶囊属性+值、系列标签）与下载按钮行统一宽度，视觉对齐一致。

## 账号页重设计

- 账号页从键值对表单重建为居中 profile 卡片：头像图标、邮箱、角色标签和修改密码按钮全部居中排列，不再是"数据库字段"感。

## 信息区美化

- 详情页元信息胶囊内文字居中，属性段统一宽度，值段等比填充。
- 系列详情页的发布年份、别名、出版社、题材、链接等信息行统一使用更精致的胶囊化标签。
- 用户管理中的角色信息同步使用统一标签样式。

## 其他改进

- Komf 高级设置从单张混排卡片拆为 5 张独立卡片，间距 12dp，视觉层级清晰。
- 离线模式页签从靠左滑动条改为 `TabRow` 三栏均分，不再"左重右轻"。
- 用户管理页"新增用户"按钮加图标并右对齐；用户卡片内操作按钮右对齐。
- 进度条在操作按钮区底部留出 4dp 间距，避免与按钮粘连。

## 版本

- 更新应用版本为 1.0.9，Android versionCode 为 19。
